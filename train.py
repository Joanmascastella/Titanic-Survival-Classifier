import torch
import helpful_functions as hf

device = hf.get_device()


def svm_train(model, optimizer, criterion, loss_list, accuracy_list, n_epochs, train_features, test_features):
    return

def knn_train(model, loss_list, accuracy_list, n_epochs, train_features, test_features):
    return